# M11-S04 — Driver export

## CURRENT TRUTH
`PLANNED` per the inspected repository baseline. Research does not promote this state.

## USER OUTCOME
Deliver Driver export as a bounded, inspectable Windows capability with truthful preflight, progress, result, and evidence.

## REAL DATA SOURCES
- System Restore
- VSS
- filesystem copy engine
- PnPUtil driver export
- Task Scheduler

## OFFICIAL APIs / INTERFACES
- System Restore
- VSS
- filesystem copy engine
- PnPUtil driver export
- Task Scheduler
- SetupAPI
- PnPUtil

## ALGORITHM / NATIVE STRATEGY
- Prefer the documented interface listed above.
- Use typed Rust/Tauri request/response structures.
- If a subprocess is required, use a fixed executable identity and allowlisted typed arguments.
- Verify post-state instead of trusting exit code alone.

## INPUTS
```json
[
  {
    "name": "scope",
    "type": "typed enum/path selector",
    "default": "service-specific safe scope",
    "validation": "must map to allowlisted service contract"
  },
  {
    "name": "advanced",
    "type": "object",
    "default": {},
    "validation": "bounded typed fields only; no raw command string"
  }
]
```

## SIMPLE UI
- choose report scope
- choose format
- redaction toggle where personal data exists
- Export

## ADVANCED UI
- include/exclude evidence sections
- timestamp/timezone
- machine identifiers policy
- pretty/compact JSON

## TERMINAL / EVIDENCE
- operation id
- phase
- data source/API
- duration
- warnings/errors
- changed/skipped objects

## OUTPUT CONTRACT
```json
{
  "operationId": "string",
  "serviceId": "M11-S04",
  "status": "completed|completed_with_warnings|failed|cancelled|planned|unavailable",
  "summary": "object",
  "evidence": "array",
  "warnings": "array",
  "changes": "array",
  "artifacts": "array",
  "undoToken": "string|null"
}
```

## PERSISTENCE / DB TABLES
Persistence:
- operation summary
- settings/profile where useful
- evidence metadata

Entities:
- backup_job
- backup_run
- backup_item
- restore_point_ref
- operation
- artifact

## SECURITY
- typed Tauri IPC only
- no arbitrary shell
- canonicalize paths
- revalidate mutable targets before mutation
- bounded subprocess arguments

## ELEVATION
read may be standard; install/remove or protected operations require elevation

## CONFIRMATION
none for read-only; explicit confirmation for mutations

## PROGRESS
MULTI_PHASE; only expose determinate percentage when total work is measurable

## CANCELLATION
Supported by research contract: `true`. The implementation must disable Cancel in unsafe phases.

## UNDO / RESTORE
Supported by research contract: `false`. Never display Restore unless a real journal/restore path exists.

## ERROR MODEL
- unsupported_os_or_api
- access_denied
- source_unavailable
- cancelled
- validation_failed
- partial_evidence

## PERFORMANCE
- stream large datasets
- bounded concurrency
- avoid unbounded in-memory accumulation

## LICENSES
- No external dependency is approved merely because it is open source; verify exact license/version before bundling.

## TESTS
- unit contract tests
- Windows integration test
- failure/permission test
- runtime smoke evidence

## RUNTIME VERIFICATION
- capture Windows build
- exact binary SHA
- input
- pre-state
- execution trace
- structured result
- post-state
- artifact/hash where applicable

## SOURCE LINKS
- https://learn.microsoft.com/en-us/windows/win32/sr/createrestorepoint-systemrestore
- https://learn.microsoft.com/en-us/windows-server/storage/file-server/volume-shadow-copy-service
- https://learn.microsoft.com/en-us/windows-hardware/drivers/devtest/pnputil-command-syntax
- https://learn.microsoft.com/en-us/windows/win32/taskschd/about-the-task-scheduler

## IMPLEMENTATION READINESS
`READY_TO_IMPLEMENT`

## IMPLEMENTATION PHASE
`P1_planned`


