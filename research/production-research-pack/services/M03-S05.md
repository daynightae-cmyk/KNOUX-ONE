# M03-S05 — Duplicate-audio detection

## CURRENT TRUTH
`PARTIAL` per the inspected repository baseline. Research does not promote this state.

## USER OUTCOME
Deliver Duplicate-audio detection as a bounded, inspectable Windows capability with truthful preflight, progress, result, and evidence.

## REAL DATA SOURCES
- Win32 file identity
- Rust streaming hash
- image decoders
- optional FFmpeg/ffprobe
- ZIP/7-Zip manifest

## OFFICIAL APIs / INTERFACES
- Win32 file identity
- Rust streaming hash
- image decoders
- optional FFmpeg/ffprobe
- ZIP/7-Zip manifest

## ALGORITHM / NATIVE STRATEGY
- Prefer the documented interface listed above.
- Use typed Rust/Tauri request/response structures.
- If a subprocess is required, use a fixed executable identity and allowlisted typed arguments.
- Verify post-state instead of trusting exit code alone.

## INPUTS
```json
[
  {
    "name": "scope",
    "type": "typed enum/path selector",
    "default": "service-specific safe scope",
    "validation": "must map to allowlisted service contract"
  },
  {
    "name": "advanced",
    "type": "object",
    "default": {},
    "validation": "bounded typed fields only; no raw command string"
  }
]
```

## SIMPLE UI
- scope/source selector
- Scan or Preview
- Review results
- Export evidence

## ADVANCED UI
- bounded filters
- timeout/result limits
- evidence verbosity

## TERMINAL / EVIDENCE
- operation id
- phase
- data source/API
- duration
- warnings/errors
- changed/skipped objects

## OUTPUT CONTRACT
```json
{
  "operationId": "string",
  "serviceId": "M03-S05",
  "status": "completed|completed_with_warnings|failed|cancelled|planned|unavailable",
  "summary": "object",
  "evidence": "array",
  "warnings": "array",
  "changes": "array",
  "artifacts": "array",
  "undoToken": "string|null"
}
```

## PERSISTENCE / DB TABLES
Persistence:
- operation summary
- settings/profile where useful
- evidence metadata

Entities:
- scan_session
- duplicate_group
- duplicate_item
- quarantine_item
- operation

## SECURITY
- typed Tauri IPC only
- no arbitrary shell
- canonicalize paths
- revalidate mutable targets before mutation
- bounded subprocess arguments

## ELEVATION
standard user unless the selected operation touches protected machine state

## CONFIRMATION
none for read-only; explicit confirmation for mutations

## PROGRESS
MULTI_PHASE; only expose determinate percentage when total work is measurable

## CANCELLATION
Supported by research contract: `true`. The implementation must disable Cancel in unsafe phases.

## UNDO / RESTORE
Supported by research contract: `false`. Never display Restore unless a real journal/restore path exists.

## ERROR MODEL
- unsupported_os_or_api
- access_denied
- source_unavailable
- cancelled
- validation_failed
- partial_evidence

## PERFORMANCE
- stream large datasets
- bounded concurrency
- avoid unbounded in-memory accumulation

## LICENSES
- No external dependency is approved merely because it is open source; verify exact license/version before bundling.

## TESTS
- unit contract tests
- Windows integration test
- failure/permission test
- runtime smoke evidence

## RUNTIME VERIFICATION
- capture Windows build
- exact binary SHA
- input
- pre-state
- execution trace
- structured result
- post-state
- artifact/hash where applicable

## SOURCE LINKS
- https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfileinformationbyhandle
- https://learn.microsoft.com/en-us/windows/win32/api/restartmanager/nf-restartmanager-rmgetlist
- https://ffmpeg.org/legal.html
- https://github.com/BLAKE3-team/BLAKE3
- https://www.7-zip.org/license.txt

## IMPLEMENTATION READINESS
`NEEDS_PROTOTYPE`

## IMPLEMENTATION PHASE
`P0_partial_closure`

## CURRENT LIMITATION
Current code decodes up to the first 300 seconds to mono 8kHz PCM, calculates roughly one-second RMS-energy bins, normalizes and hashes the quantized energy vector. It is metadata-independent but too coarse for robust near-duplicate audio and is prefix-limited.

## TARGET COMPLETE BEHAVIOR
Audio duplicate/near-duplicate discovery tolerant of tags, container/bitrate changes and modest leading/trailing silence, while keeping local processing and explainable evidence.

## TECHNICAL GAP / RECOMMENDED IMPLEMENTATION
Retain exact hash lane. For acoustic similarity, evaluate Chromaprint-style fingerprints or an equivalent permissive backend, record decoded duration, sample rate/channel normalization, and use offset-tolerant fingerprint comparison. Do not treat metadata equality as proof. If Chromaprint is adopted, compile with a dependency stack whose licensing is explicitly acceptable.

## DEPENDENCIES
- ffmpeg decode path or native decoder
- Chromaprint candidate (own code MIT; build backend licensing must be checked)
- blake3

## PARTIAL-CLOSURE TEST PLAN
- same audio different tags
- MP3/AAC/FLAC transcodes
- leading silence
- gain change
- same loudness envelope but different song
- corrupt audio
- ffmpeg absent

## PARTIAL-CLOSURE RUNTIME VERIFICATION
- fingerprint backend/version recorded
- match offset/confidence evidence
- false-positive fixture set
- offline-only path passes

