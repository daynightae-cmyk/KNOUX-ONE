# M04-S05 — Old files

## CURRENT TRUTH
`PARTIAL` per the inspected repository baseline. Research does not promote this state.

## USER OUTCOME
Deliver Old files as a bounded, inspectable Windows capability with truthful preflight, progress, result, and evidence.

## REAL DATA SOURCES
- Win32 file APIs
- GetDiskFreeSpaceEx
- Storage CIM/PowerShell
- optional USN Journal

## OFFICIAL APIs / INTERFACES
- Win32 file APIs
- GetDiskFreeSpaceEx
- Storage CIM/PowerShell
- optional USN Journal

## ALGORITHM / NATIVE STRATEGY
- Prefer the documented interface listed above.
- Use typed Rust/Tauri request/response structures.
- If a subprocess is required, use a fixed executable identity and allowlisted typed arguments.
- Verify post-state instead of trusting exit code alone.

## INPUTS
```json
[
  {
    "name": "scope",
    "type": "typed enum/path selector",
    "default": "service-specific safe scope",
    "validation": "must map to allowlisted service contract"
  },
  {
    "name": "advanced",
    "type": "object",
    "default": {},
    "validation": "bounded typed fields only; no raw command string"
  }
]
```

## SIMPLE UI
- scope/source selector
- Scan or Preview
- Review results
- Export evidence

## ADVANCED UI
- bounded filters
- timeout/result limits
- evidence verbosity

## TERMINAL / EVIDENCE
- operation id
- phase
- data source/API
- duration
- warnings/errors
- changed/skipped objects

## OUTPUT CONTRACT
```json
{
  "operationId": "string",
  "serviceId": "M04-S05",
  "status": "completed|completed_with_warnings|failed|cancelled|planned|unavailable",
  "summary": "object",
  "evidence": "array",
  "warnings": "array",
  "changes": "array",
  "artifacts": "array",
  "undoToken": "string|null"
}
```

## PERSISTENCE / DB TABLES
Persistence:
- operation summary
- settings/profile where useful
- evidence metadata

Entities:
- storage_snapshot
- scan_session
- report_export
- operation

## SECURITY
- typed Tauri IPC only
- no arbitrary shell
- canonicalize paths
- revalidate mutable targets before mutation
- bounded subprocess arguments

## ELEVATION
standard user unless the selected operation touches protected machine state

## CONFIRMATION
none for read-only; explicit confirmation for mutations

## PROGRESS
MULTI_PHASE; only expose determinate percentage when total work is measurable

## CANCELLATION
Supported by research contract: `true`. The implementation must disable Cancel in unsafe phases.

## UNDO / RESTORE
Supported by research contract: `false`. Never display Restore unless a real journal/restore path exists.

## ERROR MODEL
- unsupported_os_or_api
- access_denied
- source_unavailable
- cancelled
- validation_failed
- partial_evidence

## PERFORMANCE
- stream large datasets
- bounded concurrency
- avoid unbounded in-memory accumulation

## LICENSES
- No external dependency is approved merely because it is open source; verify exact license/version before bundling.

## TESTS
- unit contract tests
- Windows integration test
- failure/permission test
- runtime smoke evidence

## RUNTIME VERIFICATION
- capture Windows build
- exact binary SHA
- input
- pre-state
- execution trace
- structured result
- post-state
- artifact/hash where applicable

## SOURCE LINKS
- https://learn.microsoft.com/en-us/windows/win32/api/fileapi/
- https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfileinformationbyhandle
- https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/ntifs/ni-ntifs-fsctl_query_usn_journal
- https://learn.microsoft.com/en-us/powershell/module/storage/get-storagereliabilitycounter

## IMPLEMENTATION READINESS
`READY_TO_IMPLEMENT`

## IMPLEMENTATION PHASE
`P0_partial_closure`

## CURRENT LIMITATION
Repository describes last-access time with a labeled modification-time fallback. NTFS last-access updates can be system-managed/disabled and are not universally reliable as a direct 'last used' signal, so 'old' cannot mean 'unused' without qualification.

## TARGET COMPLETE BEHAVIOR
Truthful age analysis that tells the user which timestamp signal is being used and how reliable it is, supports age/size/path filters, and never labels a file 'unused' when only modification time is known.

## TECHNICAL GAP / RECOMMENDED IMPLEMENTATION
Read CreationTime/LastAccessTime/LastWriteTime through handle/file information. Detect/report the system LastAccess policy where possible. Model ageBasis = LAST_ACCESS | LAST_WRITE_FALLBACK | UNKNOWN. UI copy must say 'not accessed since' only for trustworthy LastAccess evidence; otherwise 'not modified since'. Add user-defined threshold and exclusions; read-only by default.

## DEPENDENCIES
- Win32 file APIs
- existing storage scanner
- optional SQLite historical sightings

## PARTIAL-CLOSURE TEST PLAN
- LastAccess enabled/disabled systems
- ReFS/network paths
- permission denied
- files touched during scan
- clock/timezone edge cases
- OneDrive placeholders

## PARTIAL-CLOSURE RUNTIME VERIFICATION
- every row includes ageBasis
- policy evidence stored
- no deletion action inside this service
- fixture timestamps round-trip

