# M03-S03 — Similar-image detection

## CURRENT TRUTH
`PARTIAL` per the inspected repository baseline. Research does not promote this state.

## USER OUTCOME
Deliver Similar-image detection as a bounded, inspectable Windows capability with truthful preflight, progress, result, and evidence.

## REAL DATA SOURCES
- Win32 file identity
- Rust streaming hash
- image decoders
- optional FFmpeg/ffprobe
- ZIP/7-Zip manifest

## OFFICIAL APIs / INTERFACES
- Win32 file identity
- Rust streaming hash
- image decoders
- optional FFmpeg/ffprobe
- ZIP/7-Zip manifest

## ALGORITHM / NATIVE STRATEGY
- Prefer the documented interface listed above.
- Use typed Rust/Tauri request/response structures.
- If a subprocess is required, use a fixed executable identity and allowlisted typed arguments.
- Verify post-state instead of trusting exit code alone.

## INPUTS
```json
[
  {
    "name": "scope",
    "type": "typed enum/path selector",
    "default": "service-specific safe scope",
    "validation": "must map to allowlisted service contract"
  },
  {
    "name": "advanced",
    "type": "object",
    "default": {},
    "validation": "bounded typed fields only; no raw command string"
  }
]
```

## SIMPLE UI
- scope/source selector
- Scan or Preview
- Review results
- Export evidence

## ADVANCED UI
- bounded filters
- timeout/result limits
- evidence verbosity

## TERMINAL / EVIDENCE
- operation id
- phase
- data source/API
- duration
- warnings/errors
- changed/skipped objects

## OUTPUT CONTRACT
```json
{
  "operationId": "string",
  "serviceId": "M03-S03",
  "status": "completed|completed_with_warnings|failed|cancelled|planned|unavailable",
  "summary": "object",
  "evidence": "array",
  "warnings": "array",
  "changes": "array",
  "artifacts": "array",
  "undoToken": "string|null"
}
```

## PERSISTENCE / DB TABLES
Persistence:
- operation summary
- settings/profile where useful
- evidence metadata

Entities:
- scan_session
- duplicate_group
- duplicate_item
- quarantine_item
- operation

## SECURITY
- typed Tauri IPC only
- no arbitrary shell
- canonicalize paths
- revalidate mutable targets before mutation
- bounded subprocess arguments

## ELEVATION
standard user unless the selected operation touches protected machine state

## CONFIRMATION
none for read-only; explicit confirmation for mutations

## PROGRESS
MULTI_PHASE; only expose determinate percentage when total work is measurable

## CANCELLATION
Supported by research contract: `true`. The implementation must disable Cancel in unsafe phases.

## UNDO / RESTORE
Supported by research contract: `false`. Never display Restore unless a real journal/restore path exists.

## ERROR MODEL
- unsupported_os_or_api
- access_denied
- source_unavailable
- cancelled
- validation_failed
- partial_evidence

## PERFORMANCE
- stream large datasets
- bounded concurrency
- avoid unbounded in-memory accumulation

## LICENSES
- No external dependency is approved merely because it is open source; verify exact license/version before bundling.

## TESTS
- unit contract tests
- Windows integration test
- failure/permission test
- runtime smoke evidence

## RUNTIME VERIFICATION
- capture Windows build
- exact binary SHA
- input
- pre-state
- execution trace
- structured result
- post-state
- artifact/hash where applicable

## SOURCE LINKS
- https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfileinformationbyhandle
- https://learn.microsoft.com/en-us/windows/win32/api/restartmanager/nf-restartmanager-rmgetlist
- https://ffmpeg.org/legal.html
- https://github.com/BLAKE3-team/BLAKE3
- https://www.7-zip.org/license.txt

## IMPLEMENTATION READINESS
`READY_TO_IMPLEMENT`

## IMPLEMENTATION PHASE
`P0_partial_closure`

## CURRENT LIMITATION
Current code performs full pairwise O(n²) comparison using dHash 45%, aHash 25%, RGB histogram 20%, aspect ratio 10%, union-find clustering, and full BLAKE3 evidence. It does not normalize EXIF orientation in the inspected path, has no indexed candidate stage, and emitted progress marks can_pause=false/can_cancel=false in the completion path.

## TARGET COMPLETE BEHAVIOR
Robust local similar-image discovery with orientation normalization, candidate indexing, explainable similarity signals, preview thumbnails, bounded memory, cancellation, and no destructive action until a keeper/quarantine plan is separately verified.

## TECHNICAL GAP / RECOMMENDED IMPLEMENTATION
Decode safely; normalize orientation before fingerprints; create coarse buckets by dimensions/aspect/pHash-family signature; compare candidates rather than all pairs; retain multi-signal score and individual signal breakdown. Persist fingerprint cache keyed by file identity + size + mtime; invalidate on metadata change. Consider pHash only after benchmark fixtures prove benefit over the current dHash/aHash/histogram mix.

## DEPENDENCIES
- image crate already present
- blake3 already present
- optional EXIF parser after license/maintenance review

## PARTIAL-CLOSURE TEST PLAN
- rotated EXIF pairs
- resized/recompressed JPEG pairs
- cropped/non-duplicate near scenes
- PNG/JPEG cross-format
- 10k-image performance fixture
- cancel mid-scan
- file changed during scan

## PARTIAL-CLOSURE RUNTIME VERIFICATION
- fixture truth labels vs clusters
- precision/recall report at threshold presets
- evidence includes normalized dimensions + signal scores
- cancel leaves no actionable partial group

