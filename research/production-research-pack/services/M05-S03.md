# M05-S03 — Scheduled startup tasks

## CURRENT TRUTH
`STATIC_VERIFIED` per the inspected repository baseline. Research does not promote this state.

## USER OUTCOME
Deliver Scheduled startup tasks as a bounded, inspectable Windows capability with truthful preflight, progress, result, and evidence.

## REAL DATA SOURCES
- Run/RunOnce registry
- Startup known folders
- Task Scheduler 2.0
- Service Control Manager
- WinVerifyTrust
- Event Log

## OFFICIAL APIs / INTERFACES
- Run/RunOnce registry
- Startup known folders
- Task Scheduler 2.0
- Service Control Manager
- WinVerifyTrust
- Event Log
- Task Scheduler 2.0

## ALGORITHM / NATIVE STRATEGY
- Prefer the documented interface listed above.
- Use typed Rust/Tauri request/response structures.
- If a subprocess is required, use a fixed executable identity and allowlisted typed arguments.
- Verify post-state instead of trusting exit code alone.

## INPUTS
```json
[
  {
    "name": "scope",
    "type": "typed enum/path selector",
    "default": "service-specific safe scope",
    "validation": "must map to allowlisted service contract"
  },
  {
    "name": "advanced",
    "type": "object",
    "default": {},
    "validation": "bounded typed fields only; no raw command string"
  }
]
```

## SIMPLE UI
- scope/source selector
- Scan or Preview
- Review results
- Export evidence

## ADVANCED UI
- bounded filters
- timeout/result limits
- evidence verbosity

## TERMINAL / EVIDENCE
- operation id
- phase
- data source/API
- duration
- warnings/errors
- changed/skipped objects

## OUTPUT CONTRACT
```json
{
  "operationId": "string",
  "serviceId": "M05-S03",
  "status": "completed|completed_with_warnings|failed|cancelled|planned|unavailable",
  "summary": "object",
  "evidence": "array",
  "warnings": "array",
  "changes": "array",
  "artifacts": "array",
  "undoToken": "string|null"
}
```

## PERSISTENCE / DB TABLES
Persistence:
- operation summary
- settings/profile where useful
- evidence metadata

Entities:
- startup_snapshot
- startup_entry
- operation
- evidence
- saved_profile

## SECURITY
- typed Tauri IPC only
- no arbitrary shell
- canonicalize paths
- revalidate mutable targets before mutation
- bounded subprocess arguments

## ELEVATION
standard user unless the selected operation touches protected machine state

## CONFIRMATION
show exact trigger/action and allow disable/delete

## PROGRESS
MULTI_PHASE; only expose determinate percentage when total work is measurable

## CANCELLATION
Supported by research contract: `true`. The implementation must disable Cancel in unsafe phases.

## UNDO / RESTORE
Supported by research contract: `true`. Never display Restore unless a real journal/restore path exists.

## ERROR MODEL
- unsupported_os_or_api
- access_denied
- source_unavailable
- cancelled
- validation_failed
- partial_evidence

## PERFORMANCE
- stream large datasets
- bounded concurrency
- avoid unbounded in-memory accumulation

## LICENSES
- No external dependency is approved merely because it is open source; verify exact license/version before bundling.

## TESTS
- unit contract tests
- Windows integration test
- failure/permission test
- runtime smoke evidence

## RUNTIME VERIFICATION
- capture Windows build
- exact binary SHA
- input
- pre-state
- execution trace
- structured result
- post-state
- artifact/hash where applicable

## SOURCE LINKS
- https://learn.microsoft.com/en-us/windows/win32/taskschd/about-the-task-scheduler
- https://learn.microsoft.com/en-us/windows/win32/api/wintrust/nf-wintrust-winverifytrust
- https://learn.microsoft.com/en-us/windows/win32/wes/subscribing-to-events

## IMPLEMENTATION READINESS
`READY_TO_IMPLEMENT`

## IMPLEMENTATION PHASE
`P2_existing_reference`


