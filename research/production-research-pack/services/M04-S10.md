# M04-S10 — Exportable storage reports

## CURRENT TRUTH
`PARTIAL` per the inspected repository baseline. Research does not promote this state.

## USER OUTCOME
Deliver Exportable storage reports as a bounded, inspectable Windows capability with truthful preflight, progress, result, and evidence.

## REAL DATA SOURCES
- Win32 file APIs
- GetDiskFreeSpaceEx
- Storage CIM/PowerShell
- optional USN Journal

## OFFICIAL APIs / INTERFACES
- Win32 file APIs
- GetDiskFreeSpaceEx
- Storage CIM/PowerShell
- optional USN Journal

## ALGORITHM / NATIVE STRATEGY
- Prefer the documented interface listed above.
- Use typed Rust/Tauri request/response structures.
- If a subprocess is required, use a fixed executable identity and allowlisted typed arguments.
- Verify post-state instead of trusting exit code alone.

## INPUTS
```json
[
  {
    "name": "scope",
    "type": "typed enum/path selector",
    "default": "service-specific safe scope",
    "validation": "must map to allowlisted service contract"
  },
  {
    "name": "advanced",
    "type": "object",
    "default": {},
    "validation": "bounded typed fields only; no raw command string"
  }
]
```

## SIMPLE UI
- choose report scope
- choose format
- redaction toggle where personal data exists
- Export

## ADVANCED UI
- include/exclude evidence sections
- timestamp/timezone
- machine identifiers policy
- pretty/compact JSON

## TERMINAL / EVIDENCE
- operation id
- phase
- data source/API
- duration
- warnings/errors
- changed/skipped objects

## OUTPUT CONTRACT
```json
{
  "operationId": "string",
  "serviceId": "M04-S10",
  "status": "completed|completed_with_warnings|failed|cancelled|planned|unavailable",
  "summary": "object",
  "evidence": "array",
  "warnings": "array",
  "changes": "array",
  "artifacts": "array",
  "undoToken": "string|null"
}
```

## PERSISTENCE / DB TABLES
Persistence:
- operation summary
- settings/profile where useful
- evidence metadata

Entities:
- storage_snapshot
- scan_session
- report_export
- operation

## SECURITY
- typed Tauri IPC only
- no arbitrary shell
- canonicalize paths
- revalidate mutable targets before mutation
- bounded subprocess arguments

## ELEVATION
standard user unless the selected operation touches protected machine state

## CONFIRMATION
none for read-only; explicit confirmation for mutations

## PROGRESS
MULTI_PHASE; only expose determinate percentage when total work is measurable

## CANCELLATION
Supported by research contract: `true`. The implementation must disable Cancel in unsafe phases.

## UNDO / RESTORE
Supported by research contract: `false`. Never display Restore unless a real journal/restore path exists.

## ERROR MODEL
- unsupported_os_or_api
- access_denied
- source_unavailable
- cancelled
- validation_failed
- partial_evidence

## PERFORMANCE
- stream large datasets
- bounded concurrency
- avoid unbounded in-memory accumulation

## LICENSES
- No external dependency is approved merely because it is open source; verify exact license/version before bundling.

## TESTS
- unit contract tests
- Windows integration test
- failure/permission test
- runtime smoke evidence

## RUNTIME VERIFICATION
- capture Windows build
- exact binary SHA
- input
- pre-state
- execution trace
- structured result
- post-state
- artifact/hash where applicable

## SOURCE LINKS
- https://learn.microsoft.com/en-us/windows/win32/api/fileapi/
- https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfileinformationbyhandle
- https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/ntifs/ni-ntifs-fsctl_query_usn_journal
- https://learn.microsoft.com/en-us/powershell/module/storage/get-storagereliabilitycounter

## IMPLEMENTATION READINESS
`READY_TO_IMPLEMENT`

## IMPLEMENTATION PHASE
`P0_partial_closure`

## CURRENT LIMITATION
Current repository export writes a measured in-memory storage snapshot as JSON under app data. PDF is explicitly not implemented, and snapshots are kept only in an in-memory map capped at 20, so export after process restart is not durable.

## TARGET COMPLETE BEHAVIOR
Durable report export from persisted scan evidence with JSON + CSV/HTML, and PDF only through a proven renderer. Reports must expose source scan id/SHA/time, redaction policy, warnings, and data completeness.

## TECHNICAL GAP / RECOMMENDED IMPLEMENTATION
Persist normalized storage snapshot metadata + aggregates in SQLite and optionally a compressed JSON artifact for detailed trees. Export JSON deterministically, CSV for tables, HTML for human-readable reports. Add PDF only via a vetted renderer and license plan; do not fake PDF with renamed HTML. Hash exported artifacts and store path/hash/format/size.

## DEPENDENCIES
- rusqlite already present
- serde_json already present
- optional HTML-to-PDF engine after license review

## PARTIAL-CLOSURE TEST PLAN
- export after app restart
- large scan
- Unicode/Arabic filenames
- redaction mode
- deterministic JSON
- artifact hash verification
- disk-full/write-denied

## PARTIAL-CLOSURE RUNTIME VERIFICATION
- export can be regenerated from persisted scan
- artifact SHA-256/BLAKE3 stored
- format signature validated
- source scan id and binary SHA included

